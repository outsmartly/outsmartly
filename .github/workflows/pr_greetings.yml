name: Greetings

on: [pull_request]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.ACTION_TOKEN }}
        pr-message: 'Hello **#**. Congrats on submitting your first Outsmartly PR!'
